#if DEBUG

namespace nilnul.num.real.func.src.xpn_.defer_
{
	///
	/// <see cref="func.src.IGradient"/> while this is for function the tgt of which is vector, not scalar.
	/// <summary>
	/// (∂f/∂x, ∂f/∂y)
	/// 
	/// </summary>
	/// alias:
	///		nabla, a nominal symbol to express the calculation
	///vs:
	///		gradient, which is scalar, not vector, for the tgt
	///		<see cref="nilnul.num.complex.to_.IToReal"/>
	///		<see cref="nilnul.num.complex.fn_.IToReal"/>
	///		<see cref="nilnul.geometry.planar.surface.IScalar"/>
	public interface INabla { }
}


#endif




